# P041 — Passed-all function

**Difficulty:** Hard  
**Track:** Legacy coursework: academic-management BTL  
**Topics:** UDF, NOT EXISTS, window

## Problem

Determine whether the latest attempt of every attempted assignment is passing.

## Practice contract

Solve this problem before opening the repository solution. Check not only whether the query returns rows, but also whether it is correct for duplicates, NULLs, retakes, multi-row DML, and concurrent execution when those concerns apply.

## Solution

See [`sql/03_btl/functions.sql`](../../sql/03_btl/functions.sql) and find the section marked `P041` (or the corresponding documented section for design/theory problems).
